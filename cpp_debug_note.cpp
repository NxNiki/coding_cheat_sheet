main () {
    // min() must take same data type for x and y (LC 275):
    int num = citations.size() - right;
    return min(num, citations[right]);
    
    // change data type on the run (LC 1838):
    x = (long) nums[j] * (j - i + 1))
  } 


